<div>
  <div>
    <%== @markdown.render(@car.car_sell.description) %>
  </div>
  <div class="flex gap-x-4 gap-y-5 mt-6">
    <% @car.car_sell.car_sell_images.each do |image| %>
      <div class="w-48 overflow-hidden border rounded">
        <%= image_tag(image.image.url) %>
      </div>
    <% end %>
  </div>
</div>