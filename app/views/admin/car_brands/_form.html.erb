<%= form_with(model: [:admin, car_brand]) do |form| %>
  <% if car_brand.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(car_brand.errors.count, "error") %> prohibited this car_brand from being saved:</h2>

      <ul>
        <% car_brand.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name, class: 'border' %>
  </div>

  <div class="mt-4">
    <%= form.submit(class: 'bg-red-700 px-3 py-2 text-white rounded') %>
  </div>
<% end %>
