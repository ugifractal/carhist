<div id="<%= dom_id car %>" class="mt-3 text-[#6D4AFF]">
  <p>
    <strong><%= t(".name") %> : </strong>
    <span class="text-white"><%= car.name %></span>
  </p>

  <p>
    <strong><%= t(".year") %> : </strong>
    <span class="text-white"><%= car.year %></span>
  </p>
<hr />
  <p>
    <strong><%= t(".image") %> : </strong>
  </p>
</div>
