<div class="mb-4">
  <%= link_to cars_path, class: "text-[#6D4AFF] hover:underline flex items-center gap-1" do %>
    <span class="text-lg">&larr;</span> <%= t('views.cars.back') %>
  <% end %>
</div>

<div class="mt-3">
  <% content_for :title, "Edit Fuel Filling" %>

  <h2 class="text-2xl font-semibold text-white mb-4">
    <span class="text-[#6D4AFF]">Edit</span> Fuel Filling
  </h2>

  <div class="p-5 border border-[#6D4AFF] rounded-xl shadow-sm bg-[#1A1A22]/50 backdrop-blur-md">
    <%= render partial: "car_fuelings/form" %>
  </div>
</div>
