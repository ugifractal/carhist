<div class="mb-4">
  <%= link_to car_car_maintenances_path(@car),
      class: "text-[#6D4AFF] hover:underline flex items-center gap-1" do %>
    <span class="text-lg">&larr;</span> <%= t('views.history_images.back') %>
  <% end %>
</div>

<% content_for :title, "New Car Maintenance" %>

<h1 class="text-2xl font-semibold text-white mb-4">
  <span class="text-[#6D4AFF]">New</span> Maintenance
</h1>

<div class="p-5 border border-[#6D4AFF] rounded-xl shadow-sm bg-[#1A1A22]/50 backdrop-blur-sm">
  <%= render "form", car_maintenance: @car_maintenance %>
</div>
