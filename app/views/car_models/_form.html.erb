<%= form_with(model: car_model) do |form| %>
  <% if car_model.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(car_model.errors.count, "error") %> prohibited this car from being saved:</h2>

      <ul>
        <% car_model.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form_with url: car_models_path, method: :get, local: true do %>
      <label for="brand">Pilih Merek:</label>
      <%= select_tag :brand_id, options_from_collection_for_select(CarBrand.all, 'id', 'name', params[:brand_id]), include_blank: "- Pilih Merek -" %>
      <%= form.label :model, style: "display: block" %>
    <%= form.text_field :model %>
    <%= form.label :year, style: "display: block" %>
    <%= form.number_field :year %>
      <%= submit_tag "Filter" %>
    
    
      <% end %>
  <div>
    <%= select_tag 'car_brand_id', options_from_collection_for_select(CarBrand.all, 'id', 'name'), include_blank: "- Pilih Merek -" %>
  </div>

  <div>
    <%= form.label :model, style: "display: block" %>
    <%= form.text_field :model %>
  </div>

  <div>
    <%= form.label :year, style: "display: block" %>
    <%= form.number_field :year %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
